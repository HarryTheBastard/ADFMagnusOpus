<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style TargetType="{x:Type Control}" x:Key="BaseStyle">
        <Setter Property="FontFamily" Value="{StaticResource WB.AmigaTopaz}" />
        <Setter Property="FontSize" Value="{StaticResource WB.FontSize}"/>
    </Style>

    <Style TargetType="{x:Type TextBlock}" x:Key="BaseTextBlockStyle">
        <Setter Property="FontFamily" Value="{StaticResource WB.AmigaTopaz}" />
        <Setter Property="FontSize" Value="{StaticResource WB.FontSize}"/>
    </Style>
    
    <Style x:Key="WB.Button" TargetType="{x:Type Button}" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="Foreground" Value="{StaticResource WB.Brush.Black}"/>
        <Setter Property="Background" Value="{StaticResource WB.Brush.LightGray}"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="Margin" Value="5" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">                    
                    <Border SnapsToDevicePixels="True" Background="{TemplateBinding Background}">
                        <Grid>
                            <Border BorderThickness="1" BorderBrush="{StaticResource WB.Brush.White}"/>
                            <Border BorderThickness="0,0,1,1" BorderBrush="{StaticResource WB.Brush.DarkGray}"/>
                            <ContentPresenter x:Name="PART_ContentPresenter"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{StaticResource WB.Brush.Black}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource WB.Brush.DarkGray}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="WB.TextBox" TargetType="{x:Type TextBox}" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="Background" Value="{StaticResource WB.Brush.White}"/>
        <Setter Property="BorderBrush" Value="{StaticResource WB.Brush.DarkGray}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4,2"/>
    </Style>

    <Style x:Key="WB.Menu" TargetType="{x:Type Menu}" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="Background" Value="{StaticResource WB.Brush.WindowGray}"/>
        <Setter Property="Foreground" Value="{StaticResource WB.Brush.Black}"/>
    </Style>

    <Style x:Key="WB.MenuItem" TargetType="{x:Type MenuItem}" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="Background" Value="{StaticResource WB.Brush.WindowGray}"/>
        <Setter Property="Foreground" Value="{StaticResource WB.Brush.Black}"/>
        <Setter Property="Padding" Value="8,2"/>
    </Style>

    <!-- Implicit styles -->
    <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource BaseTextBlockStyle}" />
    <Style TargetType="{x:Type Button}"   BasedOn="{StaticResource WB.Button}" />
    <Style TargetType="{x:Type TextBox}"  BasedOn="{StaticResource WB.TextBox}" />
    <Style TargetType="{x:Type Menu}"     BasedOn="{StaticResource WB.Menu}" />
    <Style TargetType="{x:Type MenuItem}" BasedOn="{StaticResource WB.MenuItem}" />
    <Style TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource BaseStyle}" />
    <Style TargetType="{x:Type ListView}" BasedOn="{StaticResource BaseStyle}" />
    <Style TargetType="{x:Type ListBox}" BasedOn="{StaticResource BaseStyle}" />
    <Style TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource BaseStyle}" />

</ResourceDictionary>
    
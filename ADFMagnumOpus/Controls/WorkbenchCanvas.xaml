<UserControl x:Class="ADFMagnumOpus.Controls.WorkbenchCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ADFMagnumOpus.Controls"
             xmlns:models="clr-namespace:ADFMagnumOpus.Models"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid Background="#2D2D2D">
        <!-- replace with your Workbench background brush -->
        <ItemsControl ItemsSource="{Binding Workbench.Icons}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding Left}"/>
                    <Setter Property="Canvas.Top" Value="{Binding Top}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.Resources>
                <DataTemplate DataType="{x:Type models:DiskImageBase}">
                    <local:FileIcon />
                </DataTemplate>
                <!-- Later you can map other file/folder view models to the same FileIcon -->
            </ItemsControl.Resources>
        </ItemsControl>
    </Grid>
</UserControl>

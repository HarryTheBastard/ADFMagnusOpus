<UserControl x:Class="ADFMagnumOpus.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel LastChildFill="True">
        <!-- Left panel -->
        <Border Width="260" Margin="0,0,6,0" Background="{StaticResource WB.Brush.LightGray}" DockPanel.Dock="Left">
            <Border BorderBrush="{StaticResource WB.Brush.White}" BorderThickness="2">
                <Border BorderBrush="{StaticResource WB.Brush.DarkGray}" BorderThickness="0,0,2,2" Padding="6">
                    <StackPanel>
                        <TextBlock Text="Images" FontWeight="Bold" FontSize="18" />
                        <ListBox Height="220" Margin="0,6,0,6">
                            <ListBoxItem>Workbench.adf</ListBoxItem>
                            <ListBoxItem>Games1.dms</ListBoxItem>
                        </ListBox>
                        <Button Content="Add…" />
                    </StackPanel>
                </Border>
            </Border>
        </Border>

        <!-- Right panel -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Background="{StaticResource WB.Brush.LightGray}" Padding="6" Margin="0,0,0,6">
                <StackPanel Orientation="Horizontal">
                    <TextBox Width="240"/>
                    <Button Content="Open" Margin="6,0,0,0"/>
                    <Button Content="Extract" Margin="6,0,0,0"/>
                    <Button Content="Insert" Margin="6,0,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Background="{StaticResource WB.Brush.LightGray}">
                <Border BorderBrush="{StaticResource WB.Brush.White}" BorderThickness="2">
                    <Border BorderBrush="{StaticResource WB.Brush.DarkGray}" BorderThickness="0,0,2,2">
                        <ListView>
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Name" Width="260" />
                                    <GridViewColumn Header="Size" Width="90" />
                                    <GridViewColumn Header="Type" Width="120" />
                                    <GridViewColumn Header="Date" Width="160" />
                                </GridView>
                            </ListView.View>
                            <ListViewItem>
                                <TextBlock Text="Empty.adf (sample row)"/>
                            </ListViewItem>
                        </ListView>
                    </Border>
                </Border>
            </Border>
        </Grid>
    </DockPanel>
</UserControl>
